- name: Install redis
  ansible.builtin.dnf:
    name: redis
    state: present

- name: Start and enable redis service
  ansible.builtin.service:
    name: redis
    state: started
    enabled: true
