---
- name: Setup Redis Backend Server
  hosts: server_role_redis_server
  become: true
  remote_user: rocky
  roles:
    - redis

- name: Setup Nginx Frontend Server
  hosts: server_role_frontend_server
  become: true
  remote_user: admin
  roles:
    - frontend
